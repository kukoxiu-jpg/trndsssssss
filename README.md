# Trans Pile à l'Heure — fixed test build

## What was fixed
- All sidebar routes now exist and load correctly.
- Client-side storage now uses safe local cache utilities with memory fallback.
- Settings save into browser cache/localStorage for testing.
- Stock, clients, suppliers, dashboard, landing page all load without missing-route errors.

## Run
npm install
npm run dev
